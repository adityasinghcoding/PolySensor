services:
  - type: web
    name: polysensor-backend
    runtime: docker
    dockerfilePath: ./Dockerfile.backend
    envVars:
      - key: GOOGLE_API_KEY
        sync: false  # Set manually in Render dashboard
      - key: FLASK_ENV
        value: production
    plan: free
